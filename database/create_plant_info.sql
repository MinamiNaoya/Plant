use plant;
create table plant_info (min_temp INTEGER NOT NULL, max_temp INTEGER , fertilizer VARCHAR(15),  Habitat VARCHAR(15), rebooting_frequency VARCHAR(5), summer_or_winter CHAR(6), purchase_date DATE , PRIMARY KEY (plant_id))
